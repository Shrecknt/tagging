<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search</title>
    <link rel="stylesheet" href="/generic_styles.css">
    <style>
        #search {
            display: block;
            width: 50%;
            height: 48px;
            position: absolute;
            top: 8px;
            left: 25%;
            text-align: center;
            outline: none;
            font-size: 24px;
        }
        #display {
            /* position: absolute; */
            margin-top: 64px;
            top: 64px;
            left: 0;
            width: 100%;
            display: block;
        }
        .container {
            width: 256px;
            height: 256px;
            position: relative;
            display: inline-block;
            margin: 4px;
            overflow: hidden;
        }
        .thumbnail {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-size: cover;
            background-position: center;
        }
        .caption {
            position: absolute;
            bottom: 0;
            left: 0;
            width: calc( 100% - 8px );
            background-color: rgba(0, 0, 0, .5);
            padding: 4px;
            overflow-x: hidden;
            color: white;
            text-align: center;
        }
        /* #scrollDetector {
            width: 16px;
            height: 16px;
            background-color: red;
        } */
    </style>
</head>

<body>
    <input type="search" id="search" placeholder="Tags">
    <div id="display"></div>
    <div id="scrollDetector"></div>
    <script src="search.js"></script>
</body>

</html>